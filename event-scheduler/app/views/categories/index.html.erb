<h3>All Categories</h3>
<% @categories.each do |category| %>
<p><strong><%=link_to category.name, category_path(category)%></strong></a>
<br><%= link_to "Create a new event in this category", new_category_event_path(category) %>
</p>
<hr>
<%end%>
</br>
